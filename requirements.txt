# No third-party packages.
#
# This tool is deliberately stdlib-only (urllib.request for HTTPS, concurrent.futures for
# concurrency, json/csv/sqlite-free caching). The evaluation agent installs from Lilly's internal
# JFrog index rather than public PyPI, where a package or version available locally is not
# guaranteed to exist -- so shipping zero dependencies removes that entire failure mode.
